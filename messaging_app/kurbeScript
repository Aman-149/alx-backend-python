#!/bin/bash
# Kubernetes cluster startup script (theoretical)

echo "Starting Minikube cluster..."
echo "minikube start"

echo "Checking cluster info..."
echo "kubectl cluster-info"

echo "Listing all pods..."
echo "kubectl get pods -A"
